import React from 'react';

const Events = (props) => {
    console.log("Events", props.user)

    return (
        <>
           <h1> Upcoming Events </h1>
        </>
    )
}

export default (Events)