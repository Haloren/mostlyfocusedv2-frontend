import React from 'react';

const Todos = (props) => {
    console.log("Todos", props.user)

    return (
        <>
            <h1> Todos List </h1>  
        </>
    )
}

export default (Todos)